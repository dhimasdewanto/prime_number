import 'package:flutter/material.dart';

class PrimeNumberView extends StatelessWidget {
  const PrimeNumberView({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Prime Number"),
    );
  }
}
